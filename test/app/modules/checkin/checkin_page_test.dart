import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('CheckinPage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(CheckinPage(title: 'Checkin')));
    //  final titleFinder = find.text('Checkin');
    //  expect(titleFinder, findsOneWidget);
  });
}
