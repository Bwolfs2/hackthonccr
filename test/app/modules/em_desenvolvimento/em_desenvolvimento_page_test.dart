import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('EmDesenvolvimentoPage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(EmDesenvolvimentoPage(title: 'EmDesenvolvimento')));
    //  final titleFinder = find.text('EmDesenvolvimento');
    //  expect(titleFinder, findsOneWidget);
  });
}
