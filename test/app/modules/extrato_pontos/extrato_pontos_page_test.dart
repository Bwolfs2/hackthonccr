import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('ExtratoPontosPage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(ExtratoPontosPage(title: 'ExtratoPontos')));
    //  final titleFinder = find.text('ExtratoPontos');
    //  expect(titleFinder, findsOneWidget);
  });
}
