import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:hackthonccr/app/modules/home/home_module.dart';

void main() {
  initModule(HomeModule());
  // ListGroupActionsController listgroupactions;
  //
  setUp(() {
    //     listgroupactions = HomeModule.to.get<ListGroupActionsController>();
  });

  group('ListGroupActionsController Test', () {
    //   test("First Test", () {
    //     expect(listgroupactions, isInstanceOf<ListGroupActionsController>());
    //   });

    //   test("Set Value", () {
    //     expect(listgroupactions.value, equals(0));
    //     listgroupactions.increment();
    //     expect(listgroupactions.value, equals(1));
    //   });
  });
}
