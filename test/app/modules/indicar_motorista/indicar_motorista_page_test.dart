import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('IndicarMotoristaPage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(IndicarMotoristaPage(title: 'IndicarMotorista')));
    //  final titleFinder = find.text('IndicarMotorista');
    //  expect(titleFinder, findsOneWidget);
  });
}
