import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('JornadaPage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(JornadaPage(title: 'Jornada')));
    //  final titleFinder = find.text('Jornada');
    //  expect(titleFinder, findsOneWidget);
  });
}
