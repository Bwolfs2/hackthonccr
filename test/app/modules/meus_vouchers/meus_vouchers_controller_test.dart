import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:hackthonccr/app/modules/meus_vouchers/meus_vouchers_module.dart';

void main() {
  initModule(MeusVouchersModule());
  // MeusVouchersController meusvouchers;
  //
  setUp(() {
    //     meusvouchers = MeusVouchersModule.to.get<MeusVouchersController>();
  });

  group('MeusVouchersController Test', () {
    //   test("First Test", () {
    //     expect(meusvouchers, isInstanceOf<MeusVouchersController>());
    //   });

    //   test("Set Value", () {
    //     expect(meusvouchers.value, equals(0));
    //     meusvouchers.increment();
    //     expect(meusvouchers.value, equals(1));
    //   });
  });
}
