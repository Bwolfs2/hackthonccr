import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('MeusVouchersPage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(MeusVouchersPage(title: 'MeusVouchers')));
    //  final titleFinder = find.text('MeusVouchers');
    //  expect(titleFinder, findsOneWidget);
  });
}
