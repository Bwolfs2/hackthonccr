import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:hackthonccr/app/modules/pagar/pagar_controller.dart';
import 'package:hackthonccr/app/modules/pagar/pagar_module.dart';

void main() {
  initModule(PagarModule());
  // PagarController pagar;
  //
  setUp(() {
    //     pagar = PagarModule.to.get<PagarController>();
  });

  group('PagarController Test', () {
    //   test("First Test", () {
    //     expect(pagar, isInstanceOf<PagarController>());
    //   });

    //   test("Set Value", () {
    //     expect(pagar.value, equals(0));
    //     pagar.increment();
    //     expect(pagar.value, equals(1));
    //   });
  });
}
