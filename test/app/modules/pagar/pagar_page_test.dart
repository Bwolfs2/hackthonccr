import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('PagarPage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(PagarPage(title: 'Pagar')));
    //  final titleFinder = find.text('Pagar');
    //  expect(titleFinder, findsOneWidget);
  });
}
