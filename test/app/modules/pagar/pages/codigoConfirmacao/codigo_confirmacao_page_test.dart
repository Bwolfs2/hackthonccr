import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('CodigoConfirmacaoPage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(CodigoConfirmacaoPage(title: 'CodigoConfirmacao')));
    //  final titleFinder = find.text('CodigoConfirmacao');
    //  expect(titleFinder, findsOneWidget);
  });
}
