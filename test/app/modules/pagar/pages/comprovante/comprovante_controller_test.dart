import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:hackthonccr/app/modules/pagar/pagar_module.dart';

void main() {
  initModule(PagarModule());
  // ComprovanteController comprovante;
  //
  setUp(() {
    //     comprovante = PagarModule.to.get<ComprovanteController>();
  });

  group('ComprovanteController Test', () {
    //   test("First Test", () {
    //     expect(comprovante, isInstanceOf<ComprovanteController>());
    //   });

    //   test("Set Value", () {
    //     expect(comprovante.value, equals(0));
    //     comprovante.increment();
    //     expect(comprovante.value, equals(1));
    //   });
  });
}
