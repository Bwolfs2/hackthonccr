import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:hackthonccr/app/modules/pagar/pagar_module.dart';

void main() {
  initModule(PagarModule());
  // ConfirmacaoController confirmacao;
  //
  setUp(() {
    //     confirmacao = PagarModule.to.get<ConfirmacaoController>();
  });

  group('ConfirmacaoController Test', () {
    //   test("First Test", () {
    //     expect(confirmacao, isInstanceOf<ConfirmacaoController>());
    //   });

    //   test("Set Value", () {
    //     expect(confirmacao.value, equals(0));
    //     confirmacao.increment();
    //     expect(confirmacao.value, equals(1));
    //   });
  });
}
