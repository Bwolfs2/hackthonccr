import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:hackthonccr/app/modules/ranking/ranking_module.dart';

void main() {
  initModule(RankingModule());
  // RankingController ranking;
  //
  setUp(() {
    //     ranking = RankingModule.to.get<RankingController>();
  });

  group('RankingController Test', () {
    //   test("First Test", () {
    //     expect(ranking, isInstanceOf<RankingController>());
    //   });

    //   test("Set Value", () {
    //     expect(ranking.value, equals(0));
    //     ranking.increment();
    //     expect(ranking.value, equals(1));
    //   });
  });
}
