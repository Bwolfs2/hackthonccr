import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:hackthonccr/app/modules/ranking/ranking_page.dart';

void main() {
  testWidgets('RankingPage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(RankingPage(title: 'Ranking')));
    //  final titleFinder = find.text('Ranking');
    //  expect(titleFinder, findsOneWidget);
  });
}
