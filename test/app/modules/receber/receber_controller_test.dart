import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:hackthonccr/app/modules/receber/receber_module.dart';

void main() {
  initModule(ReceberModule());
  // ReceberController receber;
  //
  setUp(() {
    //     receber = ReceberModule.to.get<ReceberController>();
  });

  group('ReceberController Test', () {
    //   test("First Test", () {
    //     expect(receber, isInstanceOf<ReceberController>());
    //   });

    //   test("Set Value", () {
    //     expect(receber.value, equals(0));
    //     receber.increment();
    //     expect(receber.value, equals(1));
    //   });
  });
}
