import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:hackthonccr/app/modules/receber/receber_page.dart';

void main() {
  testWidgets('ReceberPage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(ReceberPage(title: 'Receber')));
    //  final titleFinder = find.text('Receber');
    //  expect(titleFinder, findsOneWidget);
  });
}
