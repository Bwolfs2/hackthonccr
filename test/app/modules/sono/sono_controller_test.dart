import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:hackthonccr/app/modules/sono/sono_controller.dart';
import 'package:hackthonccr/app/modules/sono/sono_module.dart';

void main() {
  initModule(SonoModule());
  // SonoController sono;
  //
  setUp(() {
    //     sono = SonoModule.to.get<SonoController>();
  });

  group('SonoController Test', () {
    //   test("First Test", () {
    //     expect(sono, isInstanceOf<SonoController>());
    //   });

    //   test("Set Value", () {
    //     expect(sono.value, equals(0));
    //     sono.increment();
    //     expect(sono.value, equals(1));
    //   });
  });
}
