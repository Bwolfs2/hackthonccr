import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:hackthonccr/app/modules/sono/sono_page.dart';

void main() {
  testWidgets('SonoPage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(SonoPage(title: 'Sono')));
    //  final titleFinder = find.text('Sono');
    //  expect(titleFinder, findsOneWidget);
  });
}
