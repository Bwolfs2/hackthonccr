import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('TrocarPontosPage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(TrocarPontosPage(title: 'TrocarPontos')));
    //  final titleFinder = find.text('TrocarPontos');
    //  expect(titleFinder, findsOneWidget);
  });
}
