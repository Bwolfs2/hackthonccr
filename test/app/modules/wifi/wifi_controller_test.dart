import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:hackthonccr/app/modules/wifi/wifi_module.dart';

void main() {
  initModule(WifiModule());
  // WifiController wifi;
  //
  setUp(() {
    //     wifi = WifiModule.to.get<WifiController>();
  });

  group('WifiController Test', () {
    //   test("First Test", () {
    //     expect(wifi, isInstanceOf<WifiController>());
    //   });

    //   test("Set Value", () {
    //     expect(wifi.value, equals(0));
    //     wifi.increment();
    //     expect(wifi.value, equals(1));
    //   });
  });
}
