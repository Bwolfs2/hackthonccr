import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('WifiPage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(WifiPage(title: 'Wifi')));
    //  final titleFinder = find.text('Wifi');
    //  expect(titleFinder, findsOneWidget);
  });
}
