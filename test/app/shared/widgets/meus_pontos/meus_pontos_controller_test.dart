import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:hackthonccr/app/shared/widgets/meus_pontos/meus_pontos_controller.dart';
import 'package:hackthonccr/app/app_module.dart';

void main() {
  initModule(AppModule());
  // MeusPontosController meuspontos;
  //
  setUp(() {
    //     meuspontos = AppModule.to.get<MeusPontosController>();
  });

  group('MeusPontosController Test', () {
    //   test("First Test", () {
    //     expect(meuspontos, isInstanceOf<MeusPontosController>());
    //   });

    //   test("Set Value", () {
    //     expect(meuspontos.value, equals(0));
    //     meuspontos.increment();
    //     expect(meuspontos.value, equals(1));
    //   });
  });
}
