import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:hackthonccr/app/app_module.dart';

void main() {
  initModule(AppModule());
  // RoundedPanelController roundedpanel;
  //
  setUp(() {
    //     roundedpanel = AppModule.to.get<RoundedPanelController>();
  });

  group('RoundedPanelController Test', () {
    //   test("First Test", () {
    //     expect(roundedpanel, isInstanceOf<RoundedPanelController>());
    //   });

    //   test("Set Value", () {
    //     expect(roundedpanel.value, equals(0));
    //     roundedpanel.increment();
    //     expect(roundedpanel.value, equals(1));
    //   });
  });
}
