import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:hackthonccr/app/app_module.dart';

void main() {
  initModule(AppModule());
  // TitleController title;
  //
  setUp(() {
    //     title = AppModule.to.get<TitleController>();
  });

  group('TitleController Test', () {
    //   test("First Test", () {
    //     expect(title, isInstanceOf<TitleController>());
    //   });

    //   test("Set Value", () {
    //     expect(title.value, equals(0));
    //     title.increment();
    //     expect(title.value, equals(1));
    //   });
  });
}
